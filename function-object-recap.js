/*
--------------------
SIX CORE CONCEPTS
1. variable (declare, naming convension, variable types, primitive, math operations )
2. Conditionals (comparison: >, <, >=, <=, ===, !==, if-else, if-else, if-else)
3. Array (declare, index, get values, set values, indexOf, push, pop)
4. Loop (for loop, while loop, break, continue)
5. function(declare, parameters, return, call, use the returned value from a function)
6. Object (declare, properties, keys, values, et prop value, set value, loop object properties)

*/

/*
    1. google: function declaration vs function expression
    2. google: arrow function
    3. google: loop vs function
    4. array vs object
    5. integer vs float
    6. == vs ===
    7. differences between: var, let, const

*/
/*
    1. celsius ---> Farenhite
    2. Farenhite ---> Celsius
    3. Simple Interest: Principal, interest rate, time ---> interest amount
    4. Complex Interest:
*/
